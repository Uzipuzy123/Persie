<Window x:Class="Skua.App.WPF.QualityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Flash Quality"
        Height="360" Width="340"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Icon="{x:Null}"
        Background="#1a237e">
    <StackPanel Margin="24">

        <TextBlock Text="Flash Render Quality" Foreground="White" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
        <TextBlock Foreground="#90CAF9" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,20">
            LOW disables all anti-aliasing — faster than the in-game Low setting.
        </TextBlock>

        <Button x:Name="BtnLow"    Content="LOW"    Click="Quality_Click" Tag="LOW"
                Height="48" Margin="0,0,0,10" FontSize="15" FontWeight="Bold"
                Background="#3949ab" Foreground="White" BorderThickness="0"/>
        <Button x:Name="BtnMedium" Content="MEDIUM" Click="Quality_Click" Tag="MEDIUM"
                Height="48" Margin="0,0,0,10" FontSize="15" FontWeight="Bold"
                Background="#3949ab" Foreground="White" BorderThickness="0"/>
        <Button x:Name="BtnHigh"   Content="HIGH"   Click="Quality_Click" Tag="HIGH"
                Height="48" Margin="0,0,0,10" FontSize="15" FontWeight="Bold"
                Background="#3949ab" Foreground="White" BorderThickness="0"/>
        <Button x:Name="BtnBest"   Content="BEST"   Click="Quality_Click" Tag="BEST"
                Height="48" Margin="0,0,0,0"  FontSize="15" FontWeight="Bold"
                Background="#3949ab" Foreground="White" BorderThickness="0"/>

    </StackPanel>
</Window>
